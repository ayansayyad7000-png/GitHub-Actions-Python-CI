[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ayansayyad-calculator"
version = "1.0.0"
description = "Python GitHub Actions project by ayansayyad"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["calculator"]
